import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.photoboother.christmaslightsrouteoptimizer',
  appName: 'Christmas Lights Route Optimizer',
  webDir: 'www'
};

export default config;
